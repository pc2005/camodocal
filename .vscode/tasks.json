{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "intrinsic_calib build",
            "type": "shell",
            "command": "cd build && make -j7 intrinsic_calib",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },

        {
            "label": "extrinsic_calib build",
            "type": "shell",
            "command": "cd build && make -j7 extrinsic_calib",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}